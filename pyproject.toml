[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "polln"
version = "1.0.0"
description = "Cocapn Fleet module — polln"
readme = "README.md"
requires-python = ">=3.10"
license = {text = "MIT"}
keywords = ["cocapn", "fleet"]

[project.scripts]
polln = "mcp_codebase_search:main"
